#Create one-dimensional NumPy arrays using different generation techniques
import numpy as np

#numpy array definito da una lista
a_list = [1,2,3,4]
a_arr = np.array(a_list)
print(a_arr)

#numpy array di dimensione 1 formato da 0
b = np.zeros(4)

#array vuoto di dimensione 1
c = np.empty(4)
